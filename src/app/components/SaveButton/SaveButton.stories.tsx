import React from 'react';
import SaveButton from './SaveButton';

export default {
  title: 'Component/SaveButton',
  component: SaveButton,
};

export const Save = (): JSX.Element => <SaveButton text="Save" />;
